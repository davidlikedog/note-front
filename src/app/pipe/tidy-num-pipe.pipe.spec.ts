import { TidyNumPipePipe } from './tidy-num-pipe.pipe';

describe('TidyNumPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TidyNumPipePipe();
    expect(pipe).toBeTruthy();
  });
});
