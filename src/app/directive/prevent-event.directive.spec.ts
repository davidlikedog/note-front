import { PreventEventDirective } from './prevent-event.directive';

describe('PreventEventDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventEventDirective();
    expect(directive).toBeTruthy();
  });
});
